#include "input/ConfigurationParser.h"
#include <json/json.h>

ExperimentConfiguration ConfigurationParser::parseConfigFile(std::string filename)
{
    // Parse config file and return the config file
    // Depending on the config type the returned configuration is Logic or Experiment


}
